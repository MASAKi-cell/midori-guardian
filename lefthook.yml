pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,js,json}"
      run: rtk pnpm exec biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    build:
      run: rtk pnpm exec turbo run build
    test:
      run: rtk pnpm exec turbo run test
